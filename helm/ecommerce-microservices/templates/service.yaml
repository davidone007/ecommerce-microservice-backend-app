{{- range $key, $val := .Values.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $val.name }}
  labels:
    app: {{ $val.name }}
spec:
  selector:
    app: {{ $val.name }}
  ports:
    - protocol: TCP
      port: {{ $val.port }}
      targetPort: {{ $val.port }}
  type: ClusterIP
{{- end }}
